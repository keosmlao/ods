-- ═══════════════════════════════════════════════════════════════════════════
-- ດັດຊະນີ `ic_trans(trans_flag, doc_ref)` — ຝັ່ງ ODS
--
-- ── ເປັນຫຍັງ ──
-- ນິຍາມຂັ້ນຂອງງານຕິດຕັ້ງຕ້ອງຖາມວ່າ "**ໃບເບີກທຸກໃບ**ຂອງງານນີ້ ຊ່າງກົດຮັບແລ້ວບໍ"
-- (ເບິ່ງ lib/install-stage — ຄວາມຈິງຢູ່ແຖວເອກະສານ ບໍ່ແມ່ນທຸງ pick_finish).
-- ຄຳຖາມນັ້ນເປັນ `doc_ref in (...)` + `not exists (... doc_ref = sw.doc_no)`
-- ເຊິ່ງ**ບໍ່ມີດັດຊະນີຮອງຮັບເລີຍ** ⇒ ວັດຈິງ 06-08-2026: ນັບທົ່ວ 6,800 ໃບງານ = **42 ວິນາທີ**.
-- ຫຼັງມີດັດຊະນີ: ເບິ່ງລຸ່ມ (ຄາດ < 1 ວິນາທີ).
--
-- ຊ່ວຍ `syncErpDispatch` / `syncErpReturns` ນຳ (ທັງສອງກອງດ້ວຍ trans_flag + doc_ref).
-- ═══════════════════════════════════════════════════════════════════════════

create index if not exists idx_ic_trans_flag_doc_ref
  on ic_trans (trans_flag, doc_ref);
